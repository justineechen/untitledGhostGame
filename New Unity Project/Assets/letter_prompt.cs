using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class letter_prompt : MonoBehaviour
{

	public Image currImage;
    // Start is called before the first frame update
    void Start()
    {
		//currImage = ButtonPrompt.Load("a.png");	        
    }

    // Update is called once per frame
    void Update()
    {
        //currImage = ButtonPrompt.Load("q.png");
		//currImage = ButtonPrompt.Load("z.png");
    }
}
